// Array Iteration

// 1. for loop
let num = [1,2,3,4,5]
for (let i = 0; i<=num.length; i++)
{
    // console.log(num[i]);
}

// 2. foreach
num.forEach(allnum)
function allnum(n) {
    console.log(n);
}